EVAL_PROMPT = """
请判断以下回答进行评分。

【问题】
{query}

【标准答案】
{ground_truth}

【模型回答】
{answer}

---

评估标准：
1. 是否语义正确
2. 是否覆盖关键信息
3. 允许表达不同
4. 输出格式必须是JSON格式

---

输出JSON：
{{
  "score": 0-1
}}
"""


FAITHFULNESS_PROMPT = """
请基于给定上下文判断模型回答是否忠实于检索证据。

【问题】
{query}

【检索上下文】
{context}

【模型回答】
{answer}

---

评估标准：
1. 只能依据检索上下文判断，不要使用外部知识。
2. 1.0 表示回答中的关键结论都能被上下文直接支持，没有编造、错期、错数、错主体。
3. 0.5 表示主要方向基本正确，但存在部分无法证实、模糊表述或遗漏关键限定条件。
4. 0.0 表示关键信息与上下文冲突，或存在明显编造、错期、错数、错主体。
5. 该评分只评估“是否被上下文支持”，不评估是否与标准答案一致。
6. 输出格式必须是 JSON。

---

输出JSON：
{{
  "score": 0-1
}}
"""
